# --- Speech-to-Text ---
# Options: whisper (default), voxtral (macOS only), voxtral-streaming (macOS only)
# STT_SERVICE=voxtral-streaming

# Voxtral transcription delay in ms (must be multiple of 80, default: 480)
# Lower = faster but potentially less accurate
# VOXTRAL_DELAY_MS=480

# --- Daily transport (use -d flag) ---
# DAILY_API_KEY=
# DAILY_ROOM_URL=https://yourdomain.daily.co/room

# --- Twilio transport (use -t twilio flag) ---
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=

# --- Telnyx transport (use -t telnyx flag) ---
# TELNYX_API_KEY=

# --- Exotel transport (use -t exotel flag) ---
# EXOTEL_API_KEY=
# EXOTEL_API_TOKEN=

# --- Plivo transport (use -t plivo flag) ---
# PLIVO_AUTH_ID=
# PLIVO_AUTH_TOKEN=
